@using Sitecore.Mvc.Presentation
@using Sitecore.Mvc
@using Sitecore.Web.UI.Controls.Common.HyperlinkButtons

@model RenderingModel

<h3>Apperance</h3>

<p>The HyperlinkButton control may be used two ways:</p>

<ul>
  <li>As a <em>regular HTML hyperlink</em> to navigate to a web resource specified via the <code>NavigateUrl</code> property.</li>
  <li>As a <em>JavaScript hyperlink</em> to execute a JavaScript specified via the <code>Click</code> property.</li>
</ul>

<p>
  <strong>Notice</strong> The <code>NavigateUrl</code> has more weight than the <code>Click</code>:
  if the <code>NavigateUrl</code> property has a value, the JavaScript specified in the <code>Click</code> won't be invoked.
</p>

<p>
  <strong>Notice</strong> If the hyperlink button is disabled via the <code>IsEnabled</code> property set to <code>false</code>,
  both the <code>NavigateUrl</code> and <code>Click</code> properties are ignored.
</p>

<table class="table">
    <thead>
        <tr>
            <th>Regular HyperlinkButton</th>
            <th>Button Mode</th>
            <th>Disabled Link</th>
            <th>Invisible Link</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td>@Html.Sitecore().Controls().HyperlinkButton("Link1", new { Text = "Sitecore", NavigateUrl = "http://www.sitecore.net" })</td>
            <td>@Html.Sitecore().Controls().HyperlinkButton("Link2", new { Text = "Sitecore", NavigateUrl = "http://www.sitecore.net", IsButtonMode = true })</td>
            <td>@Html.Sitecore().Controls().HyperlinkButton("Link3", new { Text = "Wall Street Journal", NavigateUrl = "http://online.wsj.com", IsEnabled = false })</td>
            <td>@Html.Sitecore().Controls().HyperlinkButton("Link4", new { Text = "Forbes", NavigateUrl = "http://www.forbes.com", IsVisible = false })</td>
        </tr>
    </tbody>
</table>



<pre>
&#64;Html.Sitecore().Controls().HyperlinkButton("Link1", new { Text = "Sitecore", NavigateUrl = "http://www.sitecore.net" })
&#64;Html.Sitecore().Controls().HyperlinkButton("Link2", new { Text = "Sitecore", NavigateUrl = "http://www.sitecore.net", IsButtonMode = true })
&#64;Html.Sitecore().Controls().HyperlinkButton("Link3", new { Text = "Wall Street Journal", NavigateUrl = "http://online.wsj.com", IsEnabled = false })
&#64;Html.Sitecore().Controls().HyperlinkButton("Link4", new { Text = "Forbes", NavigateUrl = "http://www.forbes.com", IsVisible = false })
</pre>






<h3>Clicking</h3>
<p>
    Set the Click, Command or ServerClick property to invoke a JavaScript function, a command or a server action.
</p>


<table>
<tr>
<td>
@Html.Sitecore().Controls().HyperlinkButton("Link5", new { Text = "Click Me!", Click = "javascript:alert('Yep, I have been clicked!')" })
</td>
</tr>
</table>
<pre>
&#64;Html.Sitecore().Controls().HyperlinkButton("Link5", new { Text = "Click Me!", Click = "javascript:alert('Yep, I have been clicked!')" })
</pre>
<p>
  Disabled icon button cannot be clicked
</p>
<table>
<tr>
<td>
@Html.Sitecore().Controls().HyperlinkButton("Link6", new { Text = "Click Me!", Click = "javascript:alert('Yep, I have been clicked!')" })
</td>
</tr>
</table>
<pre>
&#64;Html.Sitecore().Controls().HyperlinkButton("Link6", new { Text = "Click Me!", Click = "javascript:alert('Yep, I have been clicked!')" })
</pre>


<h3>Data-binding</h3>
<p>
  Data-binding the <code>isEnabled</code> property.
</p>  
<table>
<tr>
    <td>
        @Html.Sitecore().Controls().HyperlinkButton("Link7", new { Text = "Click Me to Disabled this button!", Click = "javascript:this.set('isEnabled',false)" })
    </td>
</tr>
</table>
<pre>
&#64;Html.Sitecore().Controls().HyperlinkButton("Link7", new { Text = "Click Me to Disabled this button!", Click = "javascript:this.set('isEnabled',false)" })
</pre>
