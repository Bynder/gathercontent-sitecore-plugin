@using Sitecore.Data
@using Sitecore.Mvc.Presentation
@using Sitecore.Mvc
@using Sitecore.Web.UI.Controls.Common.Renderings
@model RenderingModel    
@{
    var rendering = new ID("{CA84FBFF-B3A0-4AEC-9C2C-7DDBBF7DA102}").ToString();
}
<h3>Apperance</h3>
<p>
  Expander is just a div which can be open and close.<br />
  The text parameter will be used as the title.
  The placeholder will be "Expander_ID" + ".Body".
  
</p>
<p>
    @Html.Sitecore().Controls().Rendering(this.Html, rendering, "Expander1", "/sitecore", new { Text = "An Expander",})
</p>

<pre>
&#64;Html.Sitecore().Controls().Rendering(this.Html, rendering, "Expander1", "/sitecore", new { Text = "An Expander",})
</pre>
